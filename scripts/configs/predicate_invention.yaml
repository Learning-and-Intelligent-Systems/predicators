APPROACHES:
  invent_allexclude_with_init:
    NAME: "grammar_search_invention"
    FLAGS:
      excluded_predicates: "all"
      grammar_search_max_predicates: 200
      grammar_search_pred_clusterer: "option-type-number-sample"
      grammar_search_pred_selection_approach: clustering
      strips_learner: pnad_search
      pnad_search_timeout: 600
      offline_data_method: "demo+gt_operators"
      timeout: 40
ENVS:
  cover:
    NAME: "cover"
    FLAGS:
      num_train_tasks: 200
      sesame_max_skeletons_optimized: 16
  blocks:
    NAME: "blocks"
    FLAGS:
      num_train_tasks: 200
      sesame_max_skeletons_optimized: 16
  painting:
    NAME: "painting"
    FLAGS:
      num_train_tasks: 200
      sesame_max_skeletons_optimized: 16
  tools:
    NAME: "tools"
    FLAGS:
      num_train_tasks: 1000
      sesame_max_skeletons_optimized: 1000
ARGS: []
FLAGS: {}
START_SEED: 456
NUM_SEEDS: 10