```python
def _MachineOff_holds(state: RawState, objects: Sequence[Object]
                         ) -> bool:
    """Determine if the machine is off
    """
    machine, = objects
    return state.get(machine, "is_on") <= 0.5
_MachineOff = NSPredicate("MachineOff", [_machine_type],
                                          _MachineOff_holds)

```
