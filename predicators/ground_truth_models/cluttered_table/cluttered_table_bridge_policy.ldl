(define (policy)
    (:rule Grasp
        :parameters (?can - can)
        :preconditions (and (OffendingObjects_can ?can) (HandEmpty) (Untrashed ?can))
        :goals ()
        :action (Grasp ?can)
    )
)
